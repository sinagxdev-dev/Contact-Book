def menu():
    print("--Welcome to Contact Book--")
    print("1. Add Contact")
    print("2. View Contact")
    print("3. Search Contact")
    print("4. Update Contact")
    print("5. Delete Contact")
    print("6. Exit")
    choice = input("Enter your choice: 1-6 ")

def add_contact():
    '''Add Contact '''
    name = input("Enter your name: ").lower.strip()
    phone_number = int(input("Enter your phone number: ")).strip()
    email = input("Enter your email: ex. sinagdev@gmail.com").lower().strip()

    if not name:
        print("You must input name")
        return 
    
    if not phone_number.isdigit() and phone_number != " ":
        print("Your number must be digit and cannot be empty")
        return 
    
    if email (("@" not in email and ".")) and (email != " "):
        print("Your email must have @, . and cannot be empty")
        return 
    
    if name in add_contact:
        print(f' Your name is {name} already exist')
        return

    add_contact.append([name]) = {
        "Name": name,
        "Phone": phone_number,
        "Email": email
    }
    
    print(f'Hey {name} your information is stored succesfully')
    print(f'Phone: {phone_number if phone_number else 'N/A'}')
    print(f'Email: {email if email else 'N/A'}')


    return
    
def view_contact(add_contact):
     if not add_contact:
        print("No contacts found.")
        return
count = 0
for name, info in add_contact.items():
        count += 1
        print(f"\n{count}. {name}")
        print(f"   Phone: {info['Phone']}")
        print(f"   Email: {info['Email']}")
        print(f"   Notes: {info['Notes']}")
        print("   ---------------------------")

  
print(f"\nTotal Contacts: {count}")

def search_contact(contacts):
    """Search for a contact by name."""
    print("\n=== SEARCH CONTACT ===")

    if not contacts:
        print("No contacts to search.")
        return

    search_name = input("Enter the name to search: ").strip()
    found = False

    for name, info in contacts.items():
        if search_name.lower() == name.lower():  # case-insensitive match
            print("\nContact found!")
            print(f"Name: {name}")
            print(f"Phone: {info['Phone']}")
            print(f"Email: {info['Email']}")
            print(f"Notes: {info['Notes']}")
            found = True
            break
    if not found:
        print("Contact not found.")

    input("\nPress Enter to return to the main menu...")

def main ():
    contact = {}
    while True:
print("--Welcome to Contact Book--")
print("1. Add Contact")
print("2. View Contact")
print("3. Search Contact")
print("4. Update Contact")
print("5. Delete Contact")
print("6. Exit")
choice = input("Enter your choice: 1-6 ")

if choice == 1 :
    add_contact(contact)

elif choice == 2 :
    add_view(contact)

elif choice == 3 :
   search_contact(contact)

elif choice == 4: 
    delete_contact(contact)

elif choice == 5:
    print("Thank you for using Contact Book! Now exiting!! Byee!!")

else: 
    print("Invalid input)


elif choice == 4: 
    delete_contact(contact)

elif choice == 5:
    print("Thank you for using Contact Book! Now exiting!! Byee!!")

else: 
    print("Invalid input)elif choice == 4: 
    delete_contact(contact)

elif choice == 5:
    print("Thank you for using Contact Book! Now exiting!! Byee!!")

else: 
    print("Invalid input)
elif choice == 4: 
    delete_contact(contact)

elif choice == 5:
    print("Thank you for using Contact Book! Now exiting!! Byee!!")

else: 
    print("Invalid input)elif choice == 4: 
    delete_contact(contact)

elif choice == 5:
    print("Thank you for using Contact Book! Now exiting!! Byee!!")

else: 
    print("Invalid input)

    

        
